<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <title>My Gallery</title>
  <style>
    .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px,1fr)); gap: 12px; }
    .card { border: 1px solid #ddd; padding: 8px; border-radius: 8px; background: #fafafa; }
    img { width: 100%; height: 160px; object-fit: cover; border-radius: 6px; }
    .meta { font-size: 12px; color: #555; margin-top: 6px; }
  </style>
</head>
<body>
  <h1>📸 My Gallery</h1>

  <div class="grid">
    <% images.forEach(img => { %>
      <div class="card">
        <img src="/api/gallery/file/<%= img._id %>" alt="<%= img.originalName %>" />
        <div class="meta"><%= img.originalName %></div>
      </div>
    <% }) %>
  </div>

  <p><a href="/upload">+ อัปโหลดรูปใหม่</a></p>
  <form action="/api/users/logout" method="POST"><button>Logout</button></form>
</body>
</html>
