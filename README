# Mind Gallery

เว็บแกลเลอรีสำหรับอัปโหลด/ดูรูป กดไลก์ และคอมเมนต์
รันด้วย Node.js + Express + EJS, เก็บข้อมูลใน MongoDB

## ✨ Features
- Auth (JWT) — `JWT_SECRET`
- Public/Private gallery
- ค้นหา (`?q=`) จาก `description`, `originalName`, `storedName`
- Like / Comment (แก้ไข/ลบเฉพาะเจ้าของ)
- SSR ด้วย EJS + Static assets

## 🧰 Tech Stack
- **Server:** Node.js, Express, EJS
- **DB:** MongoDB (Mongoose)
- **Test:** `npm test` Jest

## 🚀 เริ่มต้นใช้งาน

### 1) ติดตั้ง
```bash
npm install

### 2) ตั้งค่า environment variables
- คัดลอกไฟล์ `.env.example` เป็น `.env`
- แก้ไขค่าใน `.env` (เช่น `DB_URL`, `JWT_SECRET`)
- ตัวอย่าง `.env`:
  ```
  DB_URL=mongodb://localhost:27017/mind_gallery
  JWT_SECRET=change_me
  ```

### 3) รันเซิร์ฟเวอร์
```bash
node server.js

### 4) ทดสอบ
```bash
npm test

